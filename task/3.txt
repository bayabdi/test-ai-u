Что получится в результате двух вычислений? Почему?
t = (1, 2, [50, 60])
t[2] += [10, 20]

ANSWER: Код приведет к ошибке TypeError,
поскольку кортежи в Python неизменяемы,
а попытка изменить элемент внутри кортежа (t[2]) с помощью += недопустима.